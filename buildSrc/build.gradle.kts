import org.gradle.kotlin.dsl.`kotlin-dsl`

repositories {
    mavenCentral()
}

plugins {
    `kotlin-dsl`
}

/*
   Why use it?
      1. It's convenient because it uses kotlin (how?)
      2. type safety (?)
 */
